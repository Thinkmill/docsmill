import { css as style } from "@emotion/react";

export const searchItem = style({
  display: "flex",
  justifyContent: "space-between",
});

export const highlightedPart = style({
  fontWeight: "bold",
});

export const attribution = style({
  marginTop: 4,
  marginLeft: 4,
});
